# This image only needs the MCP server code and bundled skills.
# If the repository later adds other Docker build workflows that need more files,
# expand these allow rules accordingly.
*
!mcp-server/
!mcp-server/**
!mcp_server_runtime/
!mcp_server_runtime/**
!kg_agent/
!kg_agent/**
!skills/
!skills/**
